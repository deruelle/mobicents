<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>
	
	<parent>
		<artifactId>mobicents</artifactId>
		<groupId>org.mobicents</groupId>
		<version>1.2.0.GA-SNAPSHOT</version>
		<relativePath>../../pom.xml</relativePath>
	</parent>
	
	<groupId>org.mobicents.servers.media</groupId>
	<artifactId>mobicents-server-media-parent</artifactId>
	<version>1.0.0.BETA1</version>
		
	<packaging>pom</packaging>
	<name>Mobicents Media Server :: ${pom.artifactId}</name>
	
  <properties>
    <release.name>${release.name.prefix}${parent.version}${release.name.suffix}</release.name>
  </properties>   

	<modules>		
		<module>jar</module>
		<module>sar</module>
		<module>mscontrol</module>
		<module>constants</module>
		<!-- uncomment before changing version with release plugin
		<module>test</module>
		-->		
	</modules>

	<!-- uncomment for release 
	<build>
		<plugins>
			<plugin>
				<artifactId>maven-antrun-plugin</artifactId>
				<inherited>false</inherited>
				<executions>
					<execution>
						<phase>install</phase>
						<goals>
							<goal>run</goal>
						</goals>
						<configuration>
							<tasks>
								<copy overwrite="true"
									todir="${basedir}/../../release/${release.name}/jboss-${jboss.version}/server/default/deploy/mobicents-media-server-${pom.version}.sar">
									<fileset dir="${basedir}/sar/target/mobicents-media-server-${pom.version}">
										<include name="**" />
									</fileset>
								</copy>
								<copy overwrite="true"
									todir="${basedir}/../../release/${release.name}/servers/media/mobicents-media-server-${pom.version}.sar">
									<fileset dir="${basedir}/sar/target/mobicents-media-server-${pom.version}">
										<include name="**" />
									</fileset>
								</copy>
                <copy overwrite="true" file="${basedir}/release/build.xml"
                  todir="${basedir}/../../release/${release.name}/servers/media">
                  <filterset>
                    <filter token="COMPONENT_VERSION" value="mobicents-media-server-${pom.version}"/>
                  </filterset>
                </copy>
							</tasks>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>		
	</build>
	-->

</project>

<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	
	<modelVersion>4.0.0</modelVersion>
	
	<parent>
		<artifactId>mms-demo-slee</artifactId>
		<groupId>org.mobicents.servers.media.examples</groupId>
		<version>1.0.0.BETA2-SNAPSHOT</version>	
    	</parent>
	
	<artifactId>mms-demo-cnf-sbb</artifactId>
    	<name>Mobicents :: Examples :: ${pom.artifactId}</name>
	
	<dependencies>
		<dependency>	
			<artifactId>msc-ratype</artifactId>
			<groupId>org.mobicents.servers.media</groupId>
			<version>${pom.version}</version>
        	</dependency>
		<dependency>
			<groupId>${pom.groupId}</groupId>
			<artifactId>mms-demo-utility</artifactId>
			<version>${pom.version}</version>
		</dependency>
		<dependency>
			<groupId>${pom.groupId}</groupId>
			<artifactId>mms-demo-announcement-sbb</artifactId>
			<version>${pom.version}</version>
		</dependency>
	</dependencies>
	
	<!-- NEEDED TO FILTER RESOURCES WITH PROPERTIES -->
	<!-- build>
		<filters>
			<filter>${basedir}/../../example.properties</filter>
		</filters>
		<resources>
			<resource>
				<directory>src/main/resources</directory>
				<filtering>true</filtering>
				<excludes>
					<exclude>org/**</exclude>
				</excludes>
			</resource>
			<resource>
				<directory>src/main/resources</directory>
				<includes>
					<include>org/**</include>
				</includes>
			</resource>		
		</resources>
   	</build -->
   
</project>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>org.mobicents.servers.media.controllers</groupId>
		<artifactId>mobicents-media-server-controllers-parent</artifactId>
		<version>1.0.0.CR5-SNAPSHOT</version>
	</parent>

	<artifactId>mobicents-media-server-mgcpcontrol</artifactId>

	<packaging>pom</packaging>

	<name>Mobicents Media Server Controllers :: ${pom.artifactId}</name>

	<modules>
		<module>deleteconnection-sbb</module>
		<module>createconnection-sbb</module>
		<module>modifyconnection-sbb</module>
		<module>endpointconfiguration-sbb</module>
		<module>notificationrequest-sbb</module>
		<module>mgcp-sbb</module>
		<module>services-du</module>
	</modules>

	<profiles>
		<profile>
			<id>release</id>
			<properties>
				<release.dir>
					${basedir}/../../../release/${release.name}/media-server/controllers/mgcp
				</release.dir>
			</properties>
			<build>
				<plugins>
					<plugin>
						<artifactId>maven-antrun-plugin</artifactId>
						<inherited>false</inherited>
						<executions>
							<execution>
								<phase>install</phase>
								<goals>
									<goal>run</goal>
								</goals>
								<configuration>
									<tasks>
										<copy overwrite="true" todir="${release.dir}">
											<fileset dir="${basedir}/services-du/target">
												<include name="**.jar" />
												<include name="*.xml" />
												<include name="*.properties" />
											</fileset>
										</copy>
										<copy overwrite="true" file="${basedir}/services-du/target/build.xml" tofile="${release.dir}/du-management.xml" />
										<copy overwrite="true" file="${basedir}/release/build.xml" todir="${release.dir}">
											<filterset>
                                                <!-- The extra ../ is due to the ${release.dir} property defined above being bigger
                                                     than the other ${release.dir} properties defined in the controllers folder -->
												<filter token="JBOSS.PATH" value="../${jboss.path}" />
												<filter token="JBOSS.CONFIG" value="${node}" />
											</filterset>
										</copy>
									</tasks>
								</configuration>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>

</project>
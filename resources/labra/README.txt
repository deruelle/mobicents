This is simple Resource Adaptor that takes Message as input and fires it to the SBB. 
To Deploy call 'ant ra-deploy'
To Un Deploy call 'ant ra-undeploy'

To start the swing client to fire events call 'ant test-gui'

To understand the RA look at RA api
